@default = "paste"
definition {
	function paste {
		WaitForSPARefresh();

		selenium.waitForVisible();

		selenium.mouseOver();

		selenium.pause("3000");

		selenium.paste();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}