@default = "select"
definition {
	function select {
		WaitForSPARefresh();

		selenium.pause("5000");

		WaitForSPARefresh();

		selenium.waitForVisible();

		selenium.mouseOver();

		selenium.pause("3000");

		selenium.select();
	}
}