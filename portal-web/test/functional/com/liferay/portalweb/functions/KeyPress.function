@default = "keyPress"
definition {
	function keyDown {
		WaitForSPARefresh();

		selenium.mouseOver();

		selenium.pause("3000");

		selenium.keyDown();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

	function keyPress {
		WaitForSPARefresh();

		selenium.mouseOver();

		selenium.pause("3000");

		selenium.keyPress();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

	function keyPressNoError {
		WaitForSPARefresh();

		selenium.mouseOver();

		selenium.pause("3000");

		selenium.keyPress();
	}

	function keyUp {
		WaitForSPARefresh();

		selenium.mouseOver();

		selenium.pause("3000");

		selenium.keyUp();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}