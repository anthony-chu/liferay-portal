// Matches the LANGFUSE_INIT_PROJECT_* values in ../langfuse/docker-compose.yml.

export const LANGFUSE_CONFIG = {
    baseUrl: "http://localhost:3000",
    publicKey: "pk-lf-public-key",
    secretKey: "sk-lf-secret-key",
};
